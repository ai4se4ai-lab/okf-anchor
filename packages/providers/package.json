{
  "name": "@okf-anchor/providers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provider interfaces (StorageProvider, AnchorProvider, GraphProvider, Signer) and their offline local implementations. Application code depends on the interface, never a vendor SDK (CLAUDE.md §5).",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@okf-anchor/okf-core": "workspace:*",
    "kubo-rpc-client": "7.1.0",
    "n3": "^1.21.3",
    "oxigraph": "^0.4.9",
    "sparqljs": "^3.7.3"
  },
  "devDependencies": {
    "@types/n3": "^1.21.1",
    "@types/sparqljs": "^3.1.12"
  }
}
