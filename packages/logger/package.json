{
  "name": "@okf-anchor/logger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Structured, leveled, colorized console logging shared by the web app and worker. Pure formatting only: no network, no filesystem writes, no dependencies.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests"
  }
}
